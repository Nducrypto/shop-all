import { StyleSheet } from "react-native";

export const addProductStyles = StyleSheet.create({
    label: {
      color: 'black',
      fontSize: 16,
    },
    input:{
      borderWidth: 1,
      borderColor: '#ccc',
      borderRadius: 5,
      padding: 10,
      marginBottom: 10,
    },
    btnCon: {
      alignItems: 'center',
    },
  });